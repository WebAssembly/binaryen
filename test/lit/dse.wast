;; NOTE: Assertions have been generated by update_lit_checks.py and should not be edited.
;; RUN: wasm-opt -all --ldse %s -S -o - | filecheck %s

(module
  (type $struct (struct (field (mut i32))))
  (global $g (ref $struct) (struct.new $struct (i32.const 1)))

  ;; CHECK:      (func $a (type $0)
  ;; CHECK-NEXT: )
  (func $a
    (local $a (ref $struct))
    (local.set $a (global.get $g))
    (struct.set $struct 0 (local.get $a) (i32.const 2))
    (struct.set $struct 0 (local.get $a) (i32.const 3))

    ;; doesn't work with localGraph
    ;; (struct.set $struct 0 (global.get $g) (i32.const 2))
    ;; (struct.set $struct 0 (global.get $g) (i32.const 3))
  )
  ;; CHECK:      (func $b (type $0)
  ;; CHECK-NEXT: )
  (func $b)
)
