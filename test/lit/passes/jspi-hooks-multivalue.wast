;; NOTE: Assertions have been generated by update_lit_checks.py --all-items and should not be edited.

;; RUN: wasm-opt %s --enable-multivalue --enable-reference-types --enable-exception-handling --jspi-hooks --pass-arg=jspi-imports@env.pair --pass-arg=jspi-exports@main -S -o - | filecheck %s
;; RUN: wasm-opt %s --enable-multivalue --enable-reference-types --enable-exception-handling --jspi-hooks --pass-arg=jspi-imports@env.pair --pass-arg=jspi-exports@main --roundtrip -O2 -o /dev/null

;; Multivalue results are held in a tuple local.
(module
  (import "env" "pair" (func $pair (param i32) (result i32 i64)))

  ;; CHECK:      (type $0 (func (param i32) (result i32 i64)))

  ;; CHECK:      (type $1 (func (result i64)))

  ;; CHECK:      (type $2 (func (param i64 i32)))

  ;; CHECK:      (import "env" "pair" (func $byn$jspi-hooks$import$pair (param i32) (result i32 i64)))

  ;; CHECK:      (export "__jspi_enter" (func $enter))

  ;; CHECK:      (export "__jspi_exit" (func $exit))

  ;; CHECK:      (export "__jspi_suspend" (func $suspend))

  ;; CHECK:      (export "__jspi_resume" (func $resume))

  ;; CHECK:      (export "main" (func $byn$jspi-hooks$export$main))

  ;; CHECK:      (func $pair (param $0 i32) (result i32 i64)
  ;; CHECK-NEXT:  (local $1 i64)
  ;; CHECK-NEXT:  (local $2 (tuple i32 i64))
  ;; CHECK-NEXT:  (local $3 exnref)
  ;; CHECK-NEXT:  (local.set $1
  ;; CHECK-NEXT:   (call $suspend)
  ;; CHECK-NEXT:  )
  ;; CHECK-NEXT:  (local.set $3
  ;; CHECK-NEXT:   (block $byn$jspi-hooks$any (result exnref)
  ;; CHECK-NEXT:    (try_table (catch_all_ref $byn$jspi-hooks$any)
  ;; CHECK-NEXT:     (local.set $2
  ;; CHECK-NEXT:      (call $byn$jspi-hooks$import$pair
  ;; CHECK-NEXT:       (local.get $0)
  ;; CHECK-NEXT:      )
  ;; CHECK-NEXT:     )
  ;; CHECK-NEXT:    )
  ;; CHECK-NEXT:    (call $resume
  ;; CHECK-NEXT:     (local.get $1)
  ;; CHECK-NEXT:     (i32.const 0)
  ;; CHECK-NEXT:    )
  ;; CHECK-NEXT:    (return
  ;; CHECK-NEXT:     (local.get $2)
  ;; CHECK-NEXT:    )
  ;; CHECK-NEXT:   )
  ;; CHECK-NEXT:  )
  ;; CHECK-NEXT:  (call $resume
  ;; CHECK-NEXT:   (local.get $1)
  ;; CHECK-NEXT:   (i32.const 1)
  ;; CHECK-NEXT:  )
  ;; CHECK-NEXT:  (throw_ref
  ;; CHECK-NEXT:   (local.get $3)
  ;; CHECK-NEXT:  )
  ;; CHECK-NEXT: )

  ;; CHECK:      (func $enter (result i64)
  ;; CHECK-NEXT:  (i64.const 0)
  ;; CHECK-NEXT: )
  (func $enter (export "__jspi_enter") (result i64) (i64.const 0))
  ;; CHECK:      (func $exit (param $0 i64) (param $1 i32)
  ;; CHECK-NEXT: )
  (func $exit (export "__jspi_exit") (param i64 i32))
  ;; CHECK:      (func $suspend (result i64)
  ;; CHECK-NEXT:  (i64.const 0)
  ;; CHECK-NEXT: )
  (func $suspend (export "__jspi_suspend") (result i64) (i64.const 0))
  ;; CHECK:      (func $resume (param $0 i64) (param $1 i32)
  ;; CHECK-NEXT: )
  (func $resume (export "__jspi_resume") (param i64 i32))
  ;; CHECK:      (func $main (param $x i32) (result i32 i64)
  ;; CHECK-NEXT:  (call $pair
  ;; CHECK-NEXT:   (local.get $x)
  ;; CHECK-NEXT:  )
  ;; CHECK-NEXT: )
  (func $main (export "main") (param $x i32) (result i32 i64)
    (call $pair (local.get $x))
  )
)
;; CHECK:      (func $byn$jspi-hooks$export$main (param $0 i32) (result i32 i64)
;; CHECK-NEXT:  (local $1 i64)
;; CHECK-NEXT:  (local $2 (tuple i32 i64))
;; CHECK-NEXT:  (local $3 exnref)
;; CHECK-NEXT:  (local.set $1
;; CHECK-NEXT:   (call $enter)
;; CHECK-NEXT:  )
;; CHECK-NEXT:  (local.set $3
;; CHECK-NEXT:   (block $byn$jspi-hooks$any (result exnref)
;; CHECK-NEXT:    (try_table (catch_all_ref $byn$jspi-hooks$any)
;; CHECK-NEXT:     (local.set $2
;; CHECK-NEXT:      (call $main
;; CHECK-NEXT:       (local.get $0)
;; CHECK-NEXT:      )
;; CHECK-NEXT:     )
;; CHECK-NEXT:    )
;; CHECK-NEXT:    (call $exit
;; CHECK-NEXT:     (local.get $1)
;; CHECK-NEXT:     (i32.const 0)
;; CHECK-NEXT:    )
;; CHECK-NEXT:    (return
;; CHECK-NEXT:     (local.get $2)
;; CHECK-NEXT:    )
;; CHECK-NEXT:   )
;; CHECK-NEXT:  )
;; CHECK-NEXT:  (call $exit
;; CHECK-NEXT:   (local.get $1)
;; CHECK-NEXT:   (i32.const 1)
;; CHECK-NEXT:  )
;; CHECK-NEXT:  (throw_ref
;; CHECK-NEXT:   (local.get $3)
;; CHECK-NEXT:  )
;; CHECK-NEXT: )
