;; NOTE: Assertions have been generated by update_lit_checks.py and should not be edited.
;; RUN: wasm-opt %s --heap-store-optimization -all -S -o - | filecheck %s

(module
  ;; CHECK:      (type $struct (shared (struct (field (mut i32)) (field (mut i32)))))
  (type $struct (shared (struct (field (mut i32)) (field (mut i32)))))

  ;; CHECK:      (memory $mem 1 1 shared)
  (memory $mem 1 1 shared)

  ;; Test 1: Disallowed reordering (GC read NOT moved before Wasm acquire load)
  ;; CHECK:      (func $disallowed (type $1) (param $x (ref $struct)) (param $other (ref $struct))
  ;; CHECK-NEXT:  (local $ref (ref $struct))
  ;; CHECK-NEXT:  (local.set $ref
  ;; CHECK-NEXT:   (struct.new $struct
  ;; CHECK-NEXT:    (i32.const 0)
  ;; CHECK-NEXT:    (block (result i32)
  ;; CHECK-NEXT:     (drop
  ;; CHECK-NEXT:      (i32.atomic.load acqrel
  ;; CHECK-NEXT:       (i32.const 0)
  ;; CHECK-NEXT:      )
  ;; CHECK-NEXT:     )
  ;; CHECK-NEXT:     (i32.const 0)
  ;; CHECK-NEXT:    )
  ;; CHECK-NEXT:   )
  ;; CHECK-NEXT:  )
  ;; CHECK-NEXT:  (struct.set $struct 0
  ;; CHECK-NEXT:   (local.get $ref)
  ;; CHECK-NEXT:   (struct.get $struct 1
  ;; CHECK-NEXT:    (local.get $other)
  ;; CHECK-NEXT:   )
  ;; CHECK-NEXT:  )
  ;; CHECK-NEXT: )
  (func $disallowed (param $x (ref $struct)) (param $other (ref $struct))
    (local $ref (ref $struct))
    (local.set $ref
      (struct.new $struct
        (i32.const 0)
        ;; Acquire load (returns i32 via block)
        (block (result i32)
          (drop (i32.atomic.load acqrel (i32.const 0)))
          (i32.const 0)
        )
      )
    )
    ;; struct.set with GC read value.
    (struct.set $struct 0
      (local.get $ref)
      (struct.get $struct 1 (local.get $other))
    )
  )

  ;; Test 2: Allowed reordering (GC read moved before Wasm release store)
  ;; CHECK:      (func $allowed (type $1) (param $x (ref $struct)) (param $other (ref $struct))
  ;; CHECK-NEXT:  (local $ref (ref $struct))
  ;; CHECK-NEXT:  (local.set $ref
  ;; CHECK-NEXT:   (struct.new $struct
  ;; CHECK-NEXT:    (struct.get $struct 1
  ;; CHECK-NEXT:     (local.get $other)
  ;; CHECK-NEXT:    )
  ;; CHECK-NEXT:    (block (result i32)
  ;; CHECK-NEXT:     (i32.atomic.store acqrel
  ;; CHECK-NEXT:      (i32.const 0)
  ;; CHECK-NEXT:      (i32.const 42)
  ;; CHECK-NEXT:     )
  ;; CHECK-NEXT:     (i32.const 0)
  ;; CHECK-NEXT:    )
  ;; CHECK-NEXT:   )
  ;; CHECK-NEXT:  )
  ;; CHECK-NEXT:  (nop)
  ;; CHECK-NEXT: )
  (func $allowed (param $x (ref $struct)) (param $other (ref $struct))
    (local $ref (ref $struct))
    (local.set $ref
      (struct.new $struct
        (i32.const 0)
        ;; Release store (returns i32 via block)
        (block (result i32)
          (i32.atomic.store acqrel (i32.const 0) (i32.const 42))
          (i32.const 0)
        )
      )
    )
    ;; struct.set with GC read value
    (struct.set $struct 0
      (local.get $ref)
      (struct.get $struct 1 (local.get $other))
    )
  )
  ;; Test 3: Swap allowed (GC read in struct.new swapped with subsequent acquire load)
  ;; CHECK:      (func $swap_allowed (type $2) (param $other (ref $struct))
  ;; CHECK-NEXT:  (local $ref (ref $struct))
  ;; CHECK-NEXT:  (drop
  ;; CHECK-NEXT:   (i32.atomic.load acqrel
  ;; CHECK-NEXT:    (i32.const 0)
  ;; CHECK-NEXT:   )
  ;; CHECK-NEXT:  )
  ;; CHECK-NEXT:  (local.set $ref
  ;; CHECK-NEXT:   (struct.new $struct
  ;; CHECK-NEXT:    (i32.const 42)
  ;; CHECK-NEXT:    (i32.const 0)
  ;; CHECK-NEXT:   )
  ;; CHECK-NEXT:  )
  ;; CHECK-NEXT:  (nop)
  ;; CHECK-NEXT: )
  (func $swap_allowed (param $other (ref $struct))
    (local $ref (ref $struct))
    (local.set $ref
      (struct.new $struct
        (struct.get $struct 1 (local.get $other))
        (i32.const 0)
      )
    )
    (drop (i32.atomic.load acqrel (i32.const 0)))
    (struct.set $struct 0
      (local.get $ref)
      (i32.const 42)
    )
  )

  ;; Test 4: Swap disallowed (struct.new with acquire load NOT swapped with subsequent GC read)
  ;; CHECK:      (func $swap_disallowed (type $2) (param $other (ref $struct))
  ;; CHECK-NEXT:  (local $ref (ref $struct))
  ;; CHECK-NEXT:  (local.set $ref
  ;; CHECK-NEXT:   (struct.new $struct
  ;; CHECK-NEXT:    (block (result i32)
  ;; CHECK-NEXT:     (drop
  ;; CHECK-NEXT:      (i32.atomic.load acqrel
  ;; CHECK-NEXT:       (i32.const 0)
  ;; CHECK-NEXT:      )
  ;; CHECK-NEXT:     )
  ;; CHECK-NEXT:     (i32.const 0)
  ;; CHECK-NEXT:    )
  ;; CHECK-NEXT:    (i32.const 0)
  ;; CHECK-NEXT:   )
  ;; CHECK-NEXT:  )
  ;; CHECK-NEXT:  (drop
  ;; CHECK-NEXT:   (struct.get $struct 1
  ;; CHECK-NEXT:    (local.get $other)
  ;; CHECK-NEXT:   )
  ;; CHECK-NEXT:  )
  ;; CHECK-NEXT:  (struct.set $struct 0
  ;; CHECK-NEXT:   (local.get $ref)
  ;; CHECK-NEXT:   (i32.const 42)
  ;; CHECK-NEXT:  )
  ;; CHECK-NEXT: )
  (func $swap_disallowed (param $other (ref $struct))
    (local $ref (ref $struct))
    (local.set $ref
      (struct.new $struct
        (block (result i32)
          (drop (i32.atomic.load acqrel (i32.const 0)))
          (i32.const 0)
        )
        (i32.const 0)
      )
    )
    (drop (struct.get $struct 1 (local.get $other)))
    (struct.set $struct 0
      (local.get $ref)
      (i32.const 42)
    )
  )
)
