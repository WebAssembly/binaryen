;; NOTE: Assertions have been generated by update_lit_checks.py and should not be edited.
;; RUN: foreach %s %t wasm-opt -all --dae2 --closed-world -S -o - | filecheck %s

(module
  ;; CHECK:      (func $a (type $0)
  ;; CHECK-NEXT:  (call $b)
  ;; CHECK-NEXT: )
  (func $a (result i32)
    (call $b)
  )
  ;; CHECK:      (func $b (type $0)
  ;; CHECK-NEXT:  (call $a)
  ;; CHECK-NEXT: )
  (func $b (result i32)
    (call $a)
  )
  ;; CHECK:      (func $caller (type $0)
  ;; CHECK-NEXT:  (call $a)
  ;; CHECK-NEXT: )
  (func $caller
    ;; Despite the cycle, the results of $a and $b are unused and can be
    ;; removed.
    (drop (call $a))
  )
)

(module
  ;; CHECK:      (global $g (mut i32) (i32.const 0))
  (global $g (mut i32) (i32.const 0))
  ;; CHECK:      (func $a (type $0) (result i32)
  ;; CHECK-NEXT:  (call $b)
  ;; CHECK-NEXT: )
  (func $a (result i32)
    (call $b)
  )
  ;; CHECK:      (func $b (type $0) (result i32)
  ;; CHECK-NEXT:  (call $a)
  ;; CHECK-NEXT: )
  (func $b (result i32)
    (call $a)
  )
  ;; CHECK:      (func $caller (type $2)
  ;; CHECK-NEXT:  (global.set $g
  ;; CHECK-NEXT:   (call $a)
  ;; CHECK-NEXT:  )
  ;; CHECK-NEXT: )
  (func $caller
    ;; Now the result of $a is used, so the result of $b is as well.
    (global.set $g (call $a))
  )
)

(module
  ;; CHECK:      (func $a (type $0)
  ;; CHECK-NEXT:  (call $a)
  ;; CHECK-NEXT: )
  (func $a (result i32)
    ;; Self-recursion: the result can be removed.
    (call $a)
  )
  ;; CHECK:      (func $caller (type $0)
  ;; CHECK-NEXT:  (call $a)
  ;; CHECK-NEXT: )
  (func $caller
    (drop (call $a))
  )
)

(module
  ;; CHECK:      (global $g (mut i32) (i32.const 0))
  (global $g (mut i32) (i32.const 0))
  ;; CHECK:      (func $a (type $0) (result i32)
  ;; CHECK-NEXT:  (call $a)
  ;; CHECK-NEXT: )
  (func $a (result i32)
    (call $a)
  )
  ;; CHECK:      (func $caller (type $2)
  ;; CHECK-NEXT:  (global.set $g
  ;; CHECK-NEXT:   (call $a)
  ;; CHECK-NEXT:  )
  ;; CHECK-NEXT: )
  (func $caller
    ;; Same, but now the result is used.
    (global.set $g (call $a))
  )
)

(module
  ;; CHECK:      (func $a (type $0)
  ;; CHECK-NEXT:  (local $0 i32)
  ;; CHECK-NEXT:  (block
  ;; CHECK-NEXT:   (call $a)
  ;; CHECK-NEXT:  )
  ;; CHECK-NEXT:  (call $a)
  ;; CHECK-NEXT: )
  (func $a (param i32) (result i32)
    (i32.add
      (call $a
        (call $a
          (local.get 0)
        )
      )
      (local.get 0)
    )
  )

  ;; CHECK:      (func $caller (type $0)
  ;; CHECK-NEXT:  (drop
  ;; CHECK-NEXT:   (i32.const 0)
  ;; CHECK-NEXT:  )
  ;; CHECK-NEXT:  (call $a)
  ;; CHECK-NEXT: )
  (func $caller
    ;; Cycles between params and results can still be removed.
    (drop (call $a (i32.const 0)))
  )
)

(module
  ;; CHECK:      (global $g (mut i32) (i32.const 0))
  (global $g (mut i32) (i32.const 0))
  ;; CHECK:      (func $a (type $0) (param $0 i32) (result i32)
  ;; CHECK-NEXT:  (i32.add
  ;; CHECK-NEXT:   (call $a
  ;; CHECK-NEXT:    (call $a
  ;; CHECK-NEXT:     (local.get $0)
  ;; CHECK-NEXT:    )
  ;; CHECK-NEXT:   )
  ;; CHECK-NEXT:   (local.get $0)
  ;; CHECK-NEXT:  )
  ;; CHECK-NEXT: )
  (func $a (param i32) (result i32)
    (i32.add
      (call $a
        (call $a
          (local.get 0)
        )
      )
      (local.get 0)
    )
  )

  ;; CHECK:      (func $caller (type $2)
  ;; CHECK-NEXT:  (global.set $g
  ;; CHECK-NEXT:   (call $a
  ;; CHECK-NEXT:    (i32.const 0)
  ;; CHECK-NEXT:   )
  ;; CHECK-NEXT:  )
  ;; CHECK-NEXT: )
  (func $caller
    ;; Same, but now the result is used so nothing can be removed.
    (global.set $g (call $a (i32.const 0)))
  )
)
