;; NOTE: Assertions have been generated by update_lit_checks.py --all-items and should not be edited.
;; RUN: foreach %s %t wasm-opt           --type-ssa -all -S -o - | filecheck %s
;; RUN: foreach %s %t wasm-opt --nominal --type-ssa -all -S -o - | filecheck %s --check-prefix NOMINAL

;; Test in both isorecursive and nominal modes to make sure we create the new
;; types properly in both.

;; Every struct.new here should get a new type.
(module
  ;; CHECK:      (type $struct (struct (field i32)))
  ;; NOMINAL:      (type $struct (struct (field i32)))
  (type $struct (struct_subtype (field i32) data))

  ;; CHECK:      (type $none_=>_none (func))

  ;; CHECK:      (rec
  ;; CHECK-NEXT:  (type $struct$1 (struct_subtype (field i32) $struct))

  ;; CHECK:       (type $struct$2 (struct_subtype (field i32) $struct))

  ;; CHECK:       (type $struct$3 (struct_subtype (field i32) $struct))

  ;; CHECK:       (type $struct$4 (struct_subtype (field i32) $struct))

  ;; CHECK:       (type $struct$5 (struct_subtype (field i32) $struct))

  ;; CHECK:      (global $g (ref $struct) (struct.new $struct$4
  ;; CHECK-NEXT:  (i32.const 42)
  ;; CHECK-NEXT: ))
  ;; NOMINAL:      (type $none_=>_none (func))

  ;; NOMINAL:      (type $struct$4 (struct_subtype (field i32) $struct))

  ;; NOMINAL:      (type $struct$5 (struct_subtype (field i32) $struct))

  ;; NOMINAL:      (type $struct$1 (struct_subtype (field i32) $struct))

  ;; NOMINAL:      (type $struct$2 (struct_subtype (field i32) $struct))

  ;; NOMINAL:      (type $struct$3 (struct_subtype (field i32) $struct))

  ;; NOMINAL:      (global $g (ref $struct) (struct.new $struct$4
  ;; NOMINAL-NEXT:  (i32.const 42)
  ;; NOMINAL-NEXT: ))
  (global $g (ref $struct) (struct.new $struct
    (i32.const 42)
  ))

  ;; CHECK:      (global $h (ref $struct) (struct.new $struct$5
  ;; CHECK-NEXT:  (i32.const 42)
  ;; CHECK-NEXT: ))
  ;; NOMINAL:      (global $h (ref $struct) (struct.new $struct$5
  ;; NOMINAL-NEXT:  (i32.const 42)
  ;; NOMINAL-NEXT: ))
  (global $h (ref $struct) (struct.new $struct
    (i32.const 42)
  ))

  ;; CHECK:      (func $foo (type $none_=>_none)
  ;; CHECK-NEXT:  (drop
  ;; CHECK-NEXT:   (struct.new_default $struct$1)
  ;; CHECK-NEXT:  )
  ;; CHECK-NEXT:  (drop
  ;; CHECK-NEXT:   (struct.new $struct$2
  ;; CHECK-NEXT:    (i32.const 10)
  ;; CHECK-NEXT:   )
  ;; CHECK-NEXT:  )
  ;; CHECK-NEXT: )
  ;; NOMINAL:      (func $foo (type $none_=>_none)
  ;; NOMINAL-NEXT:  (drop
  ;; NOMINAL-NEXT:   (struct.new_default $struct$1)
  ;; NOMINAL-NEXT:  )
  ;; NOMINAL-NEXT:  (drop
  ;; NOMINAL-NEXT:   (struct.new $struct$2
  ;; NOMINAL-NEXT:    (i32.const 10)
  ;; NOMINAL-NEXT:   )
  ;; NOMINAL-NEXT:  )
  ;; NOMINAL-NEXT: )
  (func $foo
    (drop
      (struct.new_default $struct)
    )
    (drop
      (struct.new $struct
        (i32.const 10)
      )
    )
  )

  ;; CHECK:      (func $another-func (type $none_=>_none)
  ;; CHECK-NEXT:  (drop
  ;; CHECK-NEXT:   (struct.new $struct$3
  ;; CHECK-NEXT:    (i32.const 100)
  ;; CHECK-NEXT:   )
  ;; CHECK-NEXT:  )
  ;; CHECK-NEXT: )
  ;; NOMINAL:      (func $another-func (type $none_=>_none)
  ;; NOMINAL-NEXT:  (drop
  ;; NOMINAL-NEXT:   (struct.new $struct$3
  ;; NOMINAL-NEXT:    (i32.const 100)
  ;; NOMINAL-NEXT:   )
  ;; NOMINAL-NEXT:  )
  ;; NOMINAL-NEXT: )
  (func $another-func
    (drop
      (struct.new $struct
        (i32.const 100)
      )
    )
  )
)

;; Some of these are uninteresting and should not get a new type.
(module
  ;; CHECK:      (type $anyref_dataref_=>_none (func (param anyref dataref)))

  ;; CHECK:      (type $struct (struct (field anyref)))
  ;; NOMINAL:      (type $anyref_dataref_=>_none (func (param anyref dataref)))

  ;; NOMINAL:      (type $struct (struct (field anyref)))
  (type $struct (struct_subtype (field (ref null any)) data))

  ;; CHECK:      (rec
  ;; CHECK-NEXT:  (type $struct$1 (struct_subtype (field anyref) $struct))

  ;; CHECK:       (type $struct$2 (struct_subtype (field anyref) $struct))

  ;; CHECK:       (type $struct$3 (struct_subtype (field anyref) $struct))

  ;; CHECK:      (func $foo (type $anyref_dataref_=>_none) (param $any anyref) (param $data dataref)
  ;; CHECK-NEXT:  (drop
  ;; CHECK-NEXT:   (struct.new_default $struct$1)
  ;; CHECK-NEXT:  )
  ;; CHECK-NEXT:  (drop
  ;; CHECK-NEXT:   (struct.new $struct$2
  ;; CHECK-NEXT:    (ref.null none)
  ;; CHECK-NEXT:   )
  ;; CHECK-NEXT:  )
  ;; CHECK-NEXT:  (drop
  ;; CHECK-NEXT:   (struct.new $struct
  ;; CHECK-NEXT:    (local.get $any)
  ;; CHECK-NEXT:   )
  ;; CHECK-NEXT:  )
  ;; CHECK-NEXT:  (drop
  ;; CHECK-NEXT:   (struct.new $struct$3
  ;; CHECK-NEXT:    (local.get $data)
  ;; CHECK-NEXT:   )
  ;; CHECK-NEXT:  )
  ;; CHECK-NEXT:  (drop
  ;; CHECK-NEXT:   (block ;; (replaces something unreachable we can't emit)
  ;; CHECK-NEXT:    (drop
  ;; CHECK-NEXT:     (unreachable)
  ;; CHECK-NEXT:    )
  ;; CHECK-NEXT:    (unreachable)
  ;; CHECK-NEXT:   )
  ;; CHECK-NEXT:  )
  ;; CHECK-NEXT: )
  ;; NOMINAL:      (type $struct$1 (struct_subtype (field anyref) $struct))

  ;; NOMINAL:      (type $struct$2 (struct_subtype (field anyref) $struct))

  ;; NOMINAL:      (type $struct$3 (struct_subtype (field anyref) $struct))

  ;; NOMINAL:      (func $foo (type $anyref_dataref_=>_none) (param $any anyref) (param $data dataref)
  ;; NOMINAL-NEXT:  (drop
  ;; NOMINAL-NEXT:   (struct.new_default $struct$1)
  ;; NOMINAL-NEXT:  )
  ;; NOMINAL-NEXT:  (drop
  ;; NOMINAL-NEXT:   (struct.new $struct$2
  ;; NOMINAL-NEXT:    (ref.null none)
  ;; NOMINAL-NEXT:   )
  ;; NOMINAL-NEXT:  )
  ;; NOMINAL-NEXT:  (drop
  ;; NOMINAL-NEXT:   (struct.new $struct
  ;; NOMINAL-NEXT:    (local.get $any)
  ;; NOMINAL-NEXT:   )
  ;; NOMINAL-NEXT:  )
  ;; NOMINAL-NEXT:  (drop
  ;; NOMINAL-NEXT:   (struct.new $struct$3
  ;; NOMINAL-NEXT:    (local.get $data)
  ;; NOMINAL-NEXT:   )
  ;; NOMINAL-NEXT:  )
  ;; NOMINAL-NEXT:  (drop
  ;; NOMINAL-NEXT:   (block ;; (replaces something unreachable we can't emit)
  ;; NOMINAL-NEXT:    (drop
  ;; NOMINAL-NEXT:     (unreachable)
  ;; NOMINAL-NEXT:    )
  ;; NOMINAL-NEXT:    (unreachable)
  ;; NOMINAL-NEXT:   )
  ;; NOMINAL-NEXT:  )
  ;; NOMINAL-NEXT: )
  (func $foo (param $any (ref null any)) (param $data (ref null data))
    ;; A null is interesting.
    (drop
      (struct.new_default $struct)
    )
    (drop
      (struct.new $struct
        (ref.null none)
      )
    )
    ;; An unknown value of the same type is uninteresting.
    (drop
      (struct.new $struct
        (local.get $any)
      )
    )
    ;; But a more refined type piques our interest.
    (drop
      (struct.new $struct
        (local.get $data)
      )
    )
    ;; An unreachable is boring.
    (drop
      (struct.new $struct
        (unreachable)
      )
    )
  )
)
