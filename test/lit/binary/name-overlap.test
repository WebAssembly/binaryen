;; NOTE: Assertions have been generated by update_lit_checks.py --all-items and should not be edited.
;; RUN: wasm-opt -all %s.wasm -S -o -o | filecheck %s

;; Test that we emit deduplicated names even in the corner case that one item
;; in the names section is in the names section and the other is not, and the
;; other's temp name coincides with the name the name section gives for the
;; first.

